import React from "react";
import { View, StyleSheet } from "react-native";

interface IProps {}

const ProfileScreen: React.FC<IProps> = () => {
  return <View></View>;
};

const styles = StyleSheet.create({});

export default ProfileScreen;
